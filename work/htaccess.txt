RewriteEngine On

# Redirect *.html → clean URLs (work1.html → work1)
RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
RewriteRule ^(.+)\.html$ $1 [R=301,L]

# Internally serve clean URLs → .html
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^/]+)/?$ $1.html [L]